# 整体流程

总体分为:
1. 根据输入路径，确定输出路径


## 解题部分

1. 每次遍历前，先打印一下当前状态。
2. 根据搜索次数，打印当前进行了多少步，那些尝试进行了多少次。
3. 判断本次是不是继续
    3.1. 如果没有解决
        3.1.1. 评估当前状态
            3.1.1.1. 是否成功了， 成功就返回成功
            3.1.1.2. 记录当前深度
            3.1.1.3. 如果有解，当前是不是已经超过了解的深度   返回结束
            3.1.1.4. 深度加1
            3.1.1.5. 深度是不是超过了限制                   返回结束
            3.1.1.6. 检测当前是不是解                     
                3.1.1.6.1. 有解，是不是最优的   是就返回解决   不是就返回超出预算
                    3.1.1.6.1.1. 最优：判断是不是已经存在解，没有就返回false
                    3.1.1.6.1.2. 是不是最优解，是就返回true

                    这里求到的并不是最优秀解，他只是找到两个，然后求出一个最优解。两个取1；

4. 初次深度为0的时候，执行
    4.1. 收牌操作
        4.1.1 遍历四个foundation区域，free区域和table区域分别进行做如下操作
            (1). 排除相同类的移动
            (2). 如果为空，没有意义
            (3). 检测是否可以放入到列中
                3.1. 可以放入，进入execute
                    3.1.1. 记录类型，对牌进行编码
                    3.1.2. 将他们的深度+1
                    3.1.3. 继续深入遍历

5. 找出红黑的最小值，使得红黑尽量相差小
6. 尝试移动到收牌区域     和最开始的区别就是会加上判断。

7. 尝试部分
    7.1 尝试收牌
    7.2 自由区到工作区域      【条件桌面为null或者是free区域是13】
    7.3 table之间的             
        7.3.1 原始的列要不为null
        7.3.2 开始的长度为1才会执行   条件略多
    7.4 expore探索也是table于table之间
        7.4.1 首先原始列不可以为null
        7.4.2 遍历栈  存不存在A   并且是一张，简单说就是存在A【就跳过】
        7.4.3 expose他会根据目标是不是存在值，来区分模式

    7.5 交替连接
        7.5.1 也是table与table之间的
        7.5.2 是不是可以直接全部搬过去的
    
    7.6 将K移动到空位 是K的这一列都移动过去

    7.7 移动到空列，也是全部移动

    7.8 移动到工作区域 free

    7.9 拆分